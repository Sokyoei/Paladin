services:
  django_learning:
    build:
      context: .
      dockerfile: Dockerfile
    image: django_learning:v1
    container_name: django_learning
    ports:
      - "8001:8001"
    environment:
      - DEBUG=${DEBUG:-false}
    # volumes:
    #   - ./django_learning:/app
    restart: unless-stopped
    networks:
      - app-network

  django_nginx:
    image: nginx:alpine
    container_name: django_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./static:/app/static
      - ./templates:/app/templates
    depends_on:
      - django_learning
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
